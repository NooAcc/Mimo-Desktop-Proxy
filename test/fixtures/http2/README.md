Localhost-only, self-signed TLS certificate and public test private key for the
HTTP/2 transport tests. These are test fixtures, not production credentials.
The tests explicitly trust this certificate without disabling TLS verification.
